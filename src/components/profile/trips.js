import React, { Component } from 'react';

export default class Trips extends Component {
  constructor(props) {
    super(props);
    this.state = {

    };
  }

  render() {
    return (
      <div>I'm the fucking man (trips)</div>
    );
  }
}
